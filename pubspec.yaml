name: crud_bloc_gen
description: A package for generating CRUD BLoC code
version: 1.0.1
homepage: https://github.com/yourusername/crud_bloc_gen

environment:
  sdk: ">=3.0.0 <4.0.0" # Updated SDK constraint
  flutter: ">=3.10.0" # Added Flutter SDK constraint

dependencies:
  build: ^2.4.1
  source_gen: ^2.0.0
  analyzer: ^6.2.0
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7
  supabase_flutter: ^2.8.3
  fpdart: ^1.1.1
  get_it: ^8.0.3
  toastification: ^2.3.0
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  get: ^4.6.6
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.4.6 # Updated to latest version
  freezed: ^2.5.8
  json_serializable: ^6.9.3
  flutter_test:
    sdk: flutter
